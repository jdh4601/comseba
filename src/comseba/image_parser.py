"""ImageParser — image → structured text/JSON via Claude vision (stub)."""

from __future__ import annotations

from pathlib import Path


class ImageParser:
    def parse(self, image_path: Path, prompt: str) -> str:
        raise NotImplementedError("Implemented in COM-6")
